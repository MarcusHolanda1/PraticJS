function contar(){
    result = document.getElementById('result')

    // cont = 0

    // while (cont <= 10) {
        //     cont ++
        // }
        for (passo = 0; passo <= 10; passo++) {
            // Executa 5 vezes, com os valores de passos de 0 a 4.
            result.innerHTML += `${passo},`
        console.log('Ande um passo para o leste');
      }
}

